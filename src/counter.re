/* module Page = {
     /* TODO - create counter module */
     /* TODO - setup name, state, props, initialState */
     /* TODO - write the render function */
     /* TODO - write the handlers */
     include ReactRe.CreateComponent Counter;
     let createElement = wrapProps ();
   };

   let () = ReactDOMRe.render <Page /> (ReasonJs.Document.getElementById "index"); */
